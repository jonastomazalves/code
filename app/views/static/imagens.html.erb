<div id="slider">
  <ul>
    <% @imagens.each do |imagem| %>
      <li>
        <%= image_tag(imagem.photo.url(:medium)) %>
        <h2><%= imagem.title %></h2>
      </li>
    <% end %>
  </ul>
</div>